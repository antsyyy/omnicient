# Credentials for `docker compose up`.
#
#   cp .env.example .env    # then set a password
#
# Compose refuses to start without NEO4J_PASSWORD, so nothing is hardcoded and
# no default password ships with the repository.
NEO4J_PASSWORD=
